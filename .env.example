# Copy to .env and fill in real values. This file is safe to commit.

# GitHub personal access token — repo access for pushes / API calls
GITHUB_TOKEN=

# Vercel token — CLI deploys and project API
VERCEL_TOKEN=

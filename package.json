{
  "name": "portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "node -e \"for(const d of ['dist','.astro']) require('fs').rmSync(d,{recursive:true,force:true})\"",
    "clean:all": "node -e \"for(const d of ['dist','.astro','node_modules']) require('fs').rmSync(d,{recursive:true,force:true})\""
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
